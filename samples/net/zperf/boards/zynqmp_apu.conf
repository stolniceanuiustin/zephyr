CONFIG_NET_MGMT_EVENT_STACK_SIZE=2048

# ZCU102 uses a TI DP83867 PHY in RGMII-ID mode. Use its dedicated driver so the
# internal RGMII clock delays get programmed (the generic MII driver does not).
CONFIG_PHY_TI_DP83867=y
CONFIG_LOG=y
CONFIG_ETHERNET_LOG_LEVEL_DBG=y
CONFIG_NET_LOG=y
