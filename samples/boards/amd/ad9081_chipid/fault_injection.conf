# Fault-injection overlay. Use with:
#
#   west build -p always -b zynqmp_apu \
#       zephyr/samples/boards/amd/ad9081_chipid/ \
#       -DEXTRA_CONF_FILE=fault_injection.conf
#
# Runs the failure/recovery tests after the normal bring-up. Not for normal use
# -- it breaks a working link on purpose to see whether it comes back.
#
# SPDX-License-Identifier: Apache-2.0

CONFIG_AD9081_FAULT_INJECTION=y
